<h5>LIST</h5>

<% @stuffs.each do |stuff| %>
  <div class="article">
     <div class='article'>
    <%= image_tag stuff.image.variant(resize: '500x500'), class: 'article-image'  %>
    </div>
    <div class="article-title">
      <%= stuff.name %>
    </div>
    <div class="article-title">
      <%= stuff.info %>
    </div>
    <div class="article-title">
      <%= stuff.category.name %>
    </div>
  </div>
<% end %>